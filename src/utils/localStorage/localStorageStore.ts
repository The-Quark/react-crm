const APP_PREFIX = `${import.meta.env.VITE_APP_NAME}-app-v${import.meta.env.VITE_APP_VERSION}-dev-`;

export const LOCAL_STORAGE_CURRENCY_KEY = `${APP_PREFIX}app_currency`;
export const I18N_CONFIG_KEY = `${APP_PREFIX}i18nConfig`;
