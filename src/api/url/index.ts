const api = import.meta.env.VITE_APP_API_URL;

export const USERS_URL = `${api}/users/manage`;

export const PERMISSIONS_URL = `${api}/permissions/manage`;

export const ROLES_URL = `${api}/roles/manage`;

export const COMPANY_GLOBAL_SETTINGS_URL = `${api}/company-global-settings/manage`;

export const LANGUAGE_URL = `${api}/language/manage`;

export const CURRENCY_URL = `${api}/currency/manage`;
