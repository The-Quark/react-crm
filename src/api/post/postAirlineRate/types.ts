export interface IAirlineRateFormValues {
  airline_id: string;
  from_city_id: string;
  to_city_id: string;
  min_weight: string;
  max_weight: string;
  price_per_kg: string;
  currency: string;
  is_active: string;
}
