export interface IGlobalParamsPositionFormValues {
  company_id: string | number;
  title: string;
  description?: string;
  is_active?: boolean;
}

export interface IGlobalParamsPositionResponse {
  result: number;
}
